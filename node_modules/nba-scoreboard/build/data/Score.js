'use strict';
'use es6';

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

var _nodeEmoji = require('node-emoji');

var _nodeEmoji2 = _interopRequireDefault(_nodeEmoji);

var _immutable = require('immutable');

var _Constants = require('../constants/Constants');

var _Constants2 = _interopRequireDefault(_Constants);

var _FormattedScore = require('./FormattedScore');

var _FormattedScore2 = _interopRequireDefault(_FormattedScore);

var _Outcome = require('./Outcome');

var _Outcome2 = _interopRequireDefault(_Outcome);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

var defaults = {
  home: 0,
  away: 0
};

var Score = function (_Record) {
  _inherits(Score, _Record);

  function Score() {
    _classCallCheck(this, Score);

    return _possibleConstructorReturn(this, (Score.__proto__ || Object.getPrototypeOf(Score)).apply(this, arguments));
  }

  _createClass(Score, [{
    key: 'getOutcome',
    value: function getOutcome() {
      var scoreDifferential = this.home - this.away;

      if (scoreDifferential == 0) {
        return _Outcome2.default.TIE;
      } else if (scoreDifferential > 0) {
        return _Outcome2.default.HOME_WIN;
      }

      return _Outcome2.default.AWAY_WIN;
    }
  }, {
    key: 'format',
    value: function format() {
      var homeScore = this.home.toString();
      var awayScore = this.away.toString();

      if (this.home === _Constants2.default.ONE_HUNDRED) {
        return _nodeEmoji2.default.get(_Constants2.default.SCORE_100_EMOJI_VALUE);
      }

      var outcome = this.getOutcome();
      switch (outcome) {
        case _Outcome2.default.TIE:
          return new _FormattedScore2.default({
            home: homeScore.blue,
            away: awayScore.blue
          });

        case _Outcome2.default.AWAY_WIN:
          return new _FormattedScore2.default({
            home: homeScore.red,
            away: awayScore.green
          });

        case _Outcome2.default.HOME_WIN:
          return new _FormattedScore2.default({
            home: homeScore.green,
            away: awayScore.red
          });

        default:
          throw new ReferenceError('unknown outcome value');
      }
    }
  }]);

  return Score;
}((0, _immutable.Record)(defaults));

exports.default = Score;
;